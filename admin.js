// admin.js — Phase A hook
// Assumes existing admin logic selects standElem, standId, company

// Example usage (already wired in existing code):
// renderCallout({ standElem, standId, company, planStack, calloutSvg, lozenge, lozStand, lozCompany });
