UPLOAD INSTRUCTIONS (GitHub Pages root)

1) Upload these files into the SAME folder as your event_plan.svg (repo root):
   - admin.html
   - admin.js
   - index.html
   - index.js
   - shared.js
   - .nojekyll

2) Do NOT rename event_plan.svg. It must be exactly: event_plan.svg

3) Open:
   - Public: index.html
   - Admin:  admin.html?k=YOUR_KEY   (key is optional unless you enforce it)

Notes:
- Admin "Save" prompts for a password once per tab/session (simple client-side gate).
- Import CSV pauses polling during the import so your changes don’t get overwritten.
